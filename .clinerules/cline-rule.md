Follow `AGENTS.md` in the repository root. The same rules are repeated here so Cline applies them without opening that file.

- always verify that solution builds without warnings / errors
- update documentation, pay attention to readme.md
  - update high level folder structure if needed
  - check if tech stack needs to be refreshed
  - update dev roadmap with completed check
- when a coding task is complete, suggest a commit message in this shape (use the same shape for git commit):

What is implemented

- detail 1
- detail 2

First line states what was implemented. One blank line. Then at most 10 bullet details. No extra paragraphs.
